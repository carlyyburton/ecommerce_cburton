<div class="categories">
  <h1>Categories</h1>

  <% @categories.each do |category| %>
    <%= render category %>

    <p><%= link_to "Show products in this category", category %></p>
  <% end %>
</div>
